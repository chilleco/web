[project]
name = "tests"
version = "1.0.0"
description = "Test suite"
requires-python = ">=3.10,<3.15"
dependencies = [
    "pylint==4.0.4",
    "pytest==9.0.2",
    "pytest-asyncio==1.3.0",
    "httpx==0.28.1",  # NOTE: for FastAPI + PyTest
]

[tool.uv]
dev-dependencies = []
